from stallion.components.base import BaseCodeDeployComponent
from .base import BaseServiceSchema

class SparkHistoryServer(BaseCodeDeployComponent):
    type = 'spark-history-server'
    schema = BaseServiceSchema
    allowed_deployment_types = frozenset([ 'gae-flex' ])
